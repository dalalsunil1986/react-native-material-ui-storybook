// @flow
import { getTheme } from '../mui'

const theme = {
  palette: {
    primary: '#5C1349',
  },
  typography: {
    fontFamily: 'Raleway-Regular',
  },
  spacing: {
    buttonRadius: 18,
  },
}

export default getTheme(theme)
